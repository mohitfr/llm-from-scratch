In the previous section of this chapter of Pre-training, we looked at Cross entropy loss and
calculated the training and validation losses, 

In this section, we will be training the LLM and minimize these losses.

The following is the model configuration for this section:

GPT_CONFIG_124M = {
    "vocab_size": 50257,   # Vocabulary size
    "context_length": 256, # Shortened context length (orig: 1024)
    "emb_dim": 768,        # Embedding dimension
    "n_heads": 12,         # Number of attention heads
    "n_layers": 12,        # Number of layers
    "drop_rate": 0.1,      # Dropout rate
    "qkv_bias": False      # Query-key-value bias
}

Though i will also pre-train the the model for bigger dataset and original context length once the done with 
above configuration.

We will use the functions and classes we defined in the previous sections, all of them are present in classes.py 
in this chapter folder.


import torch
print(torch.cuda.is_available())

print(torch.cuda.get_device_name(torch.cuda.current_device()))
print(torch.cuda.device_count())
print(round(torch.cuda.get_device_properties(torch.cuda.current_device()).total_memory / (1024 ** 3), 2), "GB")